import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(()=> ({
    ul: {
        justifyContent:'space-around'
    }
})) //syntax is a function that provides a callback function that instantly returns an object